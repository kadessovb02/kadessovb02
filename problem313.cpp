#include <iostream>
#include <cmath>
using namespace std;
int main() {
    double x, r = 0, y;
    cin >> x;
    cin >> y;
        for (int i = 0; i < y; i++)
    {
        r = r + x; // without *
    }
    cout << r;
}
